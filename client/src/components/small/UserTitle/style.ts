import styled from 'styled-components';

export const Title = styled.header`
  width: 100%;
  padding: 50px 28px;
  font-size: 40px;
  font-weight: bold;
  color: var(--green);
  text-align: left;
`;
